[package]
name = "first_burn_app"
version = "0.1.0"
edition = "2021"

[dependencies]
burn = { version = "0.20.1", features = ["train", "ndarray", "wgpu"] }
burn-dataset = { version = "0.20.1", features = ["builtin-sources"] }
plotters = "0.3"
